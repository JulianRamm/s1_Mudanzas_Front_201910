/**
 * Esta clase representa a una subasta de moveasy/
 * Contiene toda la informacion relevante para una subasta
 */
export class Subasta {

    /**
     * El id de la subasta
     */
    id: number;

    
    /**
     * El valorInicial de la subasta
     */
    valorInicial: number;

     /**
     * El valorFinal de la subasta
     */
    valorFinal: number;

}